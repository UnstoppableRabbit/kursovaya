<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mobileClient.Views.ProfilePage">
    <ContentPage.Content>
        <ScrollView>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackLayout BackgroundColor="{StaticResource Accent}">
                    <Label FontSize="Header" HorizontalOptions="CenterAndExpand">Вы не вошли в систему</Label>
                </StackLayout>
                <StackLayout Grid.Row="1">
                    <Button Text="Вход" HorizontalOptions="Fill" Margin="10" Command="{Binding GoToLoginCommand}"></Button>
                    <Label FontSize="Small" HorizontalOptions="CenterAndExpand" Margin="0,40,0,0">Если у вас нет учетной записи</Label>
                    <Button Text="Регистрация" HorizontalOptions="Fill" Margin="10" Command="{Binding GoToRegisterCommand}"></Button>
                </StackLayout>
                <StackLayout></StackLayout>
            </Grid>    
        </ScrollView>
    </ContentPage.Content>
</ContentPage>